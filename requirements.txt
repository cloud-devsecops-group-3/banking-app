# ============================================================
# requirements.txt — MySQL + SQLAlchemy backend
# ============================================================

# Core framework
Flask==3.0.3

# MySQL ORM
Flask-SQLAlchemy==3.1.1

# MySQL driver (pure Python — no native libs needed)
PyMySQL==1.1.1

# Required by PyMySQL for SSL/auth support
cryptography==42.0.8

# Environment variable loading
python-dotenv==1.0.1

# Production WSGI server
gunicorn==22.0.0

# ── Optional: uncomment if switching back to Firestore ───────
# firebase-admin==6.5.0
